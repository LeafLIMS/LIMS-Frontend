import { inject, bindable, bindingMode } from 'aurelia-framework';

export class OptimisedForOrganism {
    properties_src = 'optimised_for_organism'

    activate(model) {
        this.model = model;
    }
}
